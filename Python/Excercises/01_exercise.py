# Exercise 01: Rectangle Area Calculator - Uses Notes 1-3
# Determine the area of a recetangle by getting and typecasting user input

# Typecast the measurements are floats
length = float(input("Enter the length: "))
width = float(input("Enter the width: "))

area = length * width

print(f"The area is: {area}cm²")
